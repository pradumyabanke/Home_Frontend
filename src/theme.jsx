import { createTheme } from "@mui/material";

// Define your custom theme here
const theme = createTheme({
  palette: {
    secondary: {
      main: "#ffa56c", // Your desired primary color
    },
  },
});

export default theme;
